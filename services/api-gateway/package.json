{
  "name": "api-gateway",
  "version": "1.0.0",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "test": "jest --forceExit --testTimeout=10000",
    "test:coverage": "jest --forceExit --testTimeout=10000 --coverage --collectCoverageFrom=src/**/*.js --coveragePathIgnorePatterns=src/middleware --coveragePathIgnorePatterns=src/routes --coveragePathIgnorePatterns=src/services"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.6",
    "dotenv": "^16.0.0",
    "express": "^4.19.0",
    "express-rate-limit": "^7.4.0",
    "rate-limit-redis": "^4.2.0",
    "redis": "^4.7.0",
    "swagger-ui-express": "^5.0.1"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.1.14",
    "supertest": "^7.2.2"
  }
}
